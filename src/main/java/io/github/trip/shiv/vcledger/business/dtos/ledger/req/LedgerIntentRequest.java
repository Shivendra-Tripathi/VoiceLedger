package io.github.trip.shiv.vcledger.business.dtos.ledger.req;

public interface LedgerIntentRequest { 

	/*
	 * NOTE : Each class implementing the LedgerIntentRequest directly or indirectly must implement a parse(JsonNode )
	 * method that helps in parsing and creating the request
	 */
}
